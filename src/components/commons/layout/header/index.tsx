import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: pink;
`;

export default function LayoutHeader(): JSX.Element {
  return <Wrapper>Here is Header</Wrapper>;
}
